package org.example.enums;

/*
 Los enums nos sirven para definir los valores que puede tomar una
 variable, los valores que no estan establecidos en el enum, no son
 aceptados
*/
public enum Role {
	ADMIN,
	USER,
	CLIENT
}
